# Operator console (Windows laptop). Install: pip install -r requirements.txt
PySide6>=6.6
pyzmq>=25
msgpack>=1.0
numpy
opencv-python
PyYAML>=6.0
requests

# Optional — AI overlay. Without these the console runs with raw video
# (the "AI OFF" badge) and everything else works:
#   pip install ultralytics torch --index-url https://download.pytorch.org/whl/cpu

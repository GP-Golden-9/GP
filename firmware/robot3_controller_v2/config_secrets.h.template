// Copy this file to config_secrets.h (same folder) and fill in real values.
// config_secrets.h is .gitignored — credentials never reach the repository.
#pragma once

#define WIFI_SSID     "your-ssid-here"
#define WIFI_PASSWORD "your-password-here"

// Optional: password for over-the-air firmware updates (Arduino IDE →
// network port). Defaults to "gp-inspector" if left undefined.
//#define OTA_PASSWORD  "choose-a-password"
